d3.json('topPerSubject.json').then((data) => {
    //console.log(data.flat()) // gooit alles in 1 array

   // var hoi = JSON.parse(data)
   // console.log(hoi)
    // const homeChart = d3.select('section:first-of-type')
        // .selectAll('p')
        // .data(data.flat())
        // .enter()
        // .append('p')
        // .text(data => data.subject)

//         const bars = homeChart
// .selectAll('.bar')
// .data(data.flat())
// .enter()
// .append('div')
// .classed('bar', true);
})

